项目说明：
将扫描仪扫描的多页tif图片，按每个章节自动拆分成单独的pdf文件，并将识别章节名，将pdf自动命名为章节名。
程序过程会首先将多页tif图片提取成单页的图片，然后应用opencv 的sift算法识别封面的特征点，寻找哪些图片是封面，然后应用图形学提取出封面内表格的角点，根据坐标提出标题区域的粗略截图，将此截图应用腐蚀膨胀算法提取出标题文本的精确截图，最后应用tesseract识别截图中的文本。由于tesseract默认的语言库准确率不高，需要根据自己使用的字体训练语言文件。

依赖库：
opencv
numpy
Pillow
reportlab
pytesseract